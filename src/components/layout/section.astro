---
import { cn } from '~/utils/cn'

interface Props {
  id: string
  class?: string
}

const { id, class: className } = Astro.props
---

<section id={id} class={cn('py-6 px-6', className)}>
  <div class="max-w-4xl mx-auto">
    <slot />
  </div>
</section>
