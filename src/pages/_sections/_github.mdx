import OutboundLink from '~/components/outbound-link.astro'
import UpstashIcon from '~/icons/upstash.astro'
import { starredRepos } from '~/services/github-starred'

### My latest starred repos

<OutboundLink
  href="https://upstash.com"
  class="flex items-center gap-2 text-xs no-underline"
>
  <span>Cached with</span>
  <UpstashIcon class="inline-block h-6" aria-label="Upstash" />
</OutboundLink>

{starredRepos.slice(0, 10).map((repo) => (
  <li>
    <OutboundLink href={repo.html_url}>
      {repo.full_name}
    </OutboundLink>
  </li>
))}
